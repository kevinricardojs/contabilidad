<%= form_for(@tipo_de_gasto) do |f| %>
  <% if @tipo_de_gasto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_de_gasto.errors.count, "error") %> prohibited this tipo_de_gasto from being saved:</h2>

      <ul>
      <% @tipo_de_gasto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
